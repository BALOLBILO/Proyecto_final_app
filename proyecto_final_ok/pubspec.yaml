name: proyecto_final_ok
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.19.0
  flutter_riverpod: ^2.6.1
  go_router: ^16.0.0
  firebase_core: ^2.16.0
  cloud_firestore: ^4.9.2
  http: ^1.1.0
  google_maps_flutter: ^2.7.0
  geoflutterfire2: ^2.3.15
  fl_chart: ^0.66.0
  geolocator: ^11.0.0
  geocoding: ^2.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  flutter_launcher_icons: ^0.13.1   # 👈 genera los íconos nativos

flutter:
  uses-material-design: true
  assets:
    - assets/icons/Eco_Ruta.png     # 👈 también disponible dentro de la app

# 👇 configuración del generador de íconos (Android/iOS)
flutter_launcher_icons:
  image_path: assets/icons/Eco_Ruta.png
  android: true
  ios: true
  adaptive_icon_background: "#FFFFFF"       # opcional (Android adaptive icon)
  adaptive_icon_foreground: assets/icons/Eco_Ruta.png
